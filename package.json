{
  "name": "stellar-anchor-server",
  "version": "0.0.1",
  "description": "An example implementation of a SEP-6 (and related SEPs) compliant webserver for the Stellar community",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.yaml -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "dev": "nodemon --watch src -e ts --exec npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Cordeiro (alexc@stellar.org)",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.7",
    "nodemon": "^1.19.1",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  }
}
